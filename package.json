{
    "name": "drishtimap-root",
    "version": "1.0.0",
    "description": "Root orchestration for DrishtiMap",
    "scripts": {
        "install": "npm install --prefix backend && npm install --prefix frontend",
        "build": "npm install && npm run build --prefix frontend",
        "start": "npm start --prefix backend",
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
    },
    "engines": {
        "node": ">=18.0.0"
    },
    "dependencies": {
        "concurrently": "^8.2.0"
    },
    "author": "Team Drishti",
    "license": "ISC"
}